var result = window.prompt('How many days would you like to jump?'); result = parseInt(result); var wordleBoot = window.open("https://www.nytimes.com/games/wordle/index.html", "Play Tomorrow's Wordle", "width=550,height=750" ); wordleBoot.localStorage.removeItem('nyt-wordle-state'); var tomorrow = new Date(); if(!isNaN(result)) { tomorrow.setDate(tomorrow.getDate() + result); } else { tomorrow.setDate(tomorrow.getDate() + 1); } wordleBoot.Date = new Proxy(wordleBoot.Date, { construct(target, args) { if(args.length == 0) { return tomorrow; } return new target(...args); } });
